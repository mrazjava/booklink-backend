spring:
  profiles:
    active: local
  jpa:
    show-sql: true
    database-platform: org.hibernate.dialect.H2Dialect
  flyway:
    enabled: false
  properties:
    hibernate:
      globally_quoted_identifiers: true